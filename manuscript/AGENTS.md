# Manuscript — template_sia

Tokens `{{SIA_*}}` and `{{CONFIG_TITLE}}` are injected by
`scripts/z_generate_manuscript_variables.py` after `run_sia_loop.py`.

Config block `sia:` controls task name, generation count, and live mode default.

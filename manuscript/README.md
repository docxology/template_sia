# Manuscript — template_sia

Markdown sections, `config.yaml`, and bibliography for the SIA harness exemplar. Tokens `${SIA_*}` hydrate from loop outputs via `scripts/z_generate_manuscript_variables.py`.

See [`AGENTS.md`](AGENTS.md) for section conventions.

# Mini classify task

Predict `positive` vs `negative` from `feature_0` in `train.csv`.
The reference agent writes `working/predictions.csv` with columns `id,label`.
Evaluation compares predictions against held-out labels in `data/private/labels.csv`.

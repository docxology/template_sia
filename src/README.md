# Source — template_sia

Project-specific SIA wiring: loop config, reports, manuscript tokens, and fixture replay payloads.

Logic delegates to `infrastructure.sia` for the Meta → Target → Feedback state machine.

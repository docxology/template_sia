# src/fixtures/

`recorded_generations/gen_{n}/` holds fixture replay payloads (target agent, execution log, optional results). Subdirectories under `recorded_generations/` are excluded from doc-pair lint as generated replay trees.
